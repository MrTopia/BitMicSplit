<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lunaris.btmicsplit.xposed">

    <uses-sdk android:minSdkVersion="27" android:targetSdkVersion="34" />

    <application
        android:label="BT Mic Split (Hook)"
        android:allowBackup="false">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Blocks Bluetooth SCO requests so mic stays on phone while BT audio output continues" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>
</manifest>
